"use client"
import React from 'react'
import { Button } from '../ui/button'

const Upgrade = () => {
  return (
    <Button variant={"outline"} size={"sm"}
    className='ml-2 cursor-pointer text-orange-500'>Upgrade</Button>
  )
}

export default Upgrade
